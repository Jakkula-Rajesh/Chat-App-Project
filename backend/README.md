# Chat App Backend (Mock API Server)

This is the backend for the Simplified Chat Application (Lumibyte Assignment).  
It is a lightweight Node.js + Express server that returns **mock JSON data** and does **not use any database**.

---

## 🚀 Features
- Fully mocked API responses
- Supports sessions and chat history (stored in memory)
- Returns structured tabular data for every question
- CORS enabled for frontend usage
- Clean folder structure with controllers, routes & utilities

---

## 📁 Project Structure
